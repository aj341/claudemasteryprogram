# === REQUIRED ===

# Neon Postgres (ap-southeast-2 / Sydney) — pooled connection
DATABASE_URL="postgresql://user:password@host.neon.tech/dbname?sslmode=require"

# Auth.js — generate with: openssl rand -base64 32
AUTH_SECRET=""

# Auth URL — only needed in non-Vercel environments
# AUTH_URL="https://app.commercialgrowth.com.au"

# Resend (transactional email — magic links, grading notifications)
RESEND_API_KEY=""

# Anthropic API (used by the grading worker, lessons can call Claude directly)
ANTHROPIC_API_KEY=""

# === OPTIONAL / DEFERRED ===

# Stripe (W4-T15 — checkout & subscription webhook)
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
STRIPE_PRICE_CORE=""

# PostHog (product analytics — defer to W4-T16)
NEXT_PUBLIC_POSTHOG_KEY=""
NEXT_PUBLIC_POSTHOG_HOST="https://us.i.posthog.com"
